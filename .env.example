# Backend API base URL (no trailing slash). Use this when running the Flask backend locally.
# VITE_API_URL=http://localhost:5000

# Set to "true" to use in-memory mock API (no backend required).
# Set to "false" when using the Flask backend (and set VITE_API_URL above).
VITE_USE_MOCK=false
